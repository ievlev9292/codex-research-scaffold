# Prepare a research report

Reconcile the existing run task list with the intended report. Include supported outcomes and material limitations; record deliberate omissions. Check equations, values, conventions and claim strength against supporting notes and calculations.

1. Draft timestamped .tex source in a self-contained reports_tex/ bundle. Do not compile the report or render its new diagrams yet. The main agent explicitly considers whether TikZ pictures would help understanding and includes them when useful, using the installed tikz-diagrams skill. Record the brief diagram decision in the existing RUN.md; no diagram quota or separate planning document.
2. Before the first compilation, obtain independent source review from at least one separate agent that did not author the report, unless the user explicitly waives review. Use the installed ARS academic-research-suite router and its academic-paper-reviewer workflow, in a mode appropriate to the report's scope. This standing request authorizes bounded reviewer delegation without another permission checkpoint. Supply the TeX, research request and source/calculation evidence. Review claims, assumptions, equations, numerical checks, citations and task coverage; state uninspected scope. Source review cannot certify rendered appearance.
3. Correct material findings or state justified scientific limitations. Have substantive revisions rechecked. Resolve source-review blockers before compiling; preserve the review-before-rendering sequence when applying ARS and TikZ instructions.
4. Compile using the required TeX engine and bibliography route, then inspect actual PDF pages, references and figures non-interactively. Fix compilation/layout issues and inspect affected output again. Scientific changes made during layout corrections require independent re-review. Tie source-review completion and page-inspection results to the delivered version.

Use exact configured executables with workdir at the source or auxiliary-file directory on hosts that require direct invocation. Do not silently substitute engines or install missing packages; use MiKTeX --disable-installer when installation is not authorized.

Computational figures stay with their generating execution. Copy the exact included rendered version into the report's figures/ for preservation. Its INDEX.md identifies filename/label, source artifact, generating script/version, execution/data reference and modifications. Link detailed uncertainty and processing choices. Report-only TikZ sources may live here; inline diagrams identify their TeX location. Existing calculation plots need not be regenerated for source review.

Record concise review findings/disposition, version and unresolved limitations in RUN.md, linking evidence rather than requiring a separate dossier. If ARS, a needed TikZ skill or an independent reviewer is unavailable, record the missing capability and keep the affected review/diagram step pending; report it at the next natural update and continue unaffected work. Do not silently substitute self-review, claim skill use, or install dependencies. User-authorized exceptions are recorded as exceptions; waived review is not passed review.

Register the finished report through report_locator.py and verify representative equation/section lookups against the actual artifact. Unknown parser coverage is not successful mapping. Use tools/check_project.py for mechanical links and figure coverage; it cannot establish scientific correctness.

Prose polishing through de-ai-polish-writing remains explicit-only. Report preparation neither closes the run nor authorizes UI previews.
